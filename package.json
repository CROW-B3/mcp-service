{
	"name": "mcp-service",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "workers-mcp docgen src/index.ts && wrangler deploy",
		"dev": "wrangler dev --env local --port 8011 --inspector-port 9240",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.71",
		"typescript": "^5.9.3",
		"vitest": "~3.2.4",
		"wrangler": "^4.57.0"
	},
	"dependencies": {
		"workers-mcp": "^0.0.13"
	}
}
